PORT=4000
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# PostgreSQL connection string (e.g. for database named freight-ai)
DATABASE_URL=postgresql://user:password@localhost:5432/freight-ai

# Upload storage: final approved documents
UPLOAD_DIR=uploads
# Pending uploads (before user approval)
PENDING_UPLOAD_DIR=uploads/pending